export enum BoardTasksActions {
    LOAD_TASKS_FOR_BOARD = '[BoardTasksList] laod tasks for board',
    LOAD_TASKS_FOR_BOARD_SUCCESS = '[BoardTasksList] load tasks for board success',
    DEFAULT_FILTERED_TASKS_ON_LOAD = '[BoardTasksList] set default filter on load page',
    FILTER_TASKS = '[BoardTasksList] set filter tasks',
    SET_FIELD_TASK_STATUS = '[BoardTasksList] update filter tasks field '
}